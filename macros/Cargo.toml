[package]
name = "bevy_action_map_macros"
description = "Derive macros for bevy_action_map. Re-exported by that crate; do not depend on it directly."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }

[lints]
workspace = true
